{
  "name": "ascua-sitio",
  "private": true,
  "type": "module",
  "description": "Sitio de Ascua: HTML generado por el propio framework, servido por Vite",
  "scripts": {
    "dev": "vite",
    "build": "./build.sh && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
