{
  "name": "contador-ts",
  "private": true,
  "type": "module",
  "description": "Ascua con superficie TypeScript: HTML en el template, Rust solo en el compilador.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "size": "node medir.mjs"
  },
  "devDependencies": {
    "vite": "^8.3.0",
    "terser": "^5.51.2"
  }
}
