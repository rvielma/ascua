{
  "name": "ssr-ts",
  "private": true,
  "type": "module",
  "description": "Páginas renderizadas en el servidor con islas que se hidratan en el cliente.",
  "scripts": {
    "dev": "node servidor.js",
    "build": "vite build --outDir dist/cliente && vite build --ssr src/entrada-servidor.ts --outDir dist/servidor",
    "start": "NODE_ENV=production node servidor.js",
    "test": "vitest run",
    "check": "node ../../packages/check/bin/ascua-check.js"
  },
  "devDependencies": {
    "vite": "^8.3.0",
    "terser": "^5.51.2",
    "vitest": "^5.0.1",
    "happy-dom": "^20.14.5",
    "typescript": "^7.0.2"
  }
}
